const Checkout = () => {
    return (
        <div>
            <h2>Checkout</h2>
            <button onClick={() => alert("Order placed!")}>Place Order</button>
        </div>
    );
};

export default Checkout;
